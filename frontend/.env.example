# Base URL of a running AI Tutor backend (e.g. http://localhost:3000).
# Leave this blank or unset to run the frontend in self-contained DEMO MODE,
# which serves built-in mock data and requires no backend. Demo mode is what
# powers the Vercel showcase deployment.
NEXT_PUBLIC_API_URL=
